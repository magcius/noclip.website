
language: node_js
node_js:
  - "node"

script:
  - parcel build index.html
  - rm -r dist/data/
  - mv data/ dist/

deploy:
  provider: pages
  skip_cleanup: true
  keep_history: true
  local_dir: dist
  github_token: $GITHUB_TOKEN
  on:
    branch: master
